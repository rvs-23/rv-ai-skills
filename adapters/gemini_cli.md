# Gemini CLI Adapter

To use these skills in Gemini CLI:
1. Run `bash setup.sh` to install and link all discoverable skills globally to `~/.gemini/skills/`.
2. Add project-specific instructions to a local `GEMINI.md` file if needed.
